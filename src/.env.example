ANTHROPIC_API_KEY=your-anthropic-api-key-here
# IBM Bob (Claude) API key — OPTIONAL
# The pipeline works fully without this; IBM Bob is used for AI-generated
# plain-English maintenance advisories. Without a key, deterministic
# template-based fallbacks are used instead.
# Get your key at: https://console.anthropic.com/
